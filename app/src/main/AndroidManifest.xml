<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cs410project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cs410Project">
        <activity android:name=".volunteeringCertificates"></activity>
        <activity android:name=".coursesCertificates" />
        <activity android:name=".AcademicCertificates" />
        <activity android:name=".Logout" />
        <activity android:name=".certificatesMenu" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".addCertificate" />
        <activity android:name=".signup" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>